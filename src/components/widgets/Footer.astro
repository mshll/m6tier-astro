---
import { SITE } from '~/config.mjs';
// import { getHomePermalink } from '~/utils/permalinks';
// import { buttonVariants } from '~/components/ui/button';

import { socialData } from '~/data';
import Socials from '../Socials.astro';
import Link from '~/components/Link.astro';

// const { socialLinks = [], secondaryLinks = [], links = [], footNote = '', theme = 'light' } = Astro.props;
---

<footer
  class="bottom-0 flex w-full flex-col items-center justify-around space-x-3 space-y-5 border-t border-border p-4 text-center text-xs text-muted-foreground sm:flex-row sm:space-y-0"
>
  <div class="flex flex-row space-x-1">
    <p>© 2023 {SITE.name}.</p>
    <p>
      by
      <Link href="https://mshl.me">
        <strong>mshl.me</strong>
      </Link>.
    </p>
  </div>
  <Socials {...socialData} size="w-5 h-5" class="space-x-4" />
</footer>
